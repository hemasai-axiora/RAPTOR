{
    "InstanceIds": ["i-0b68a2d9c74586653"],
    "DocumentName": "AWS-RunShellScript",
    "Parameters": {
        "commands": [
            "cd /var/www/html",
            "git fetch origin main",
            "git reset --hard origin/main",
            "docker exec raptor-web php bin/migrate.php || php bin/migrate.php || true",
            "docker exec raptor-web php migrations/0037_backfill_social_analytics_history.php || php migrations/0037_backfill_social_analytics_history.php || true",
            "systemctl restart apache2 || systemctl restart nginx || docker-compose restart || true"
        ]
    }
}
